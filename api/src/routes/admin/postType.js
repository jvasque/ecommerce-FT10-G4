const router = require('express').Router();
const {
    User,
    Order,
    OrderDetail,
    Product,
    PaymentMethod,
  } = require("../../db");

module.exports = (req, res, next){
    const token = req.User;
    
    try{

    }catch{

    }
}